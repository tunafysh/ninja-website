import { useEffect } from "react"

/**
 * Renders a square blue block styled for layout.
 *
 * @returns A div element with Tailwind classes `w-20`, `bg-blue-500`, and `aspect-square`.
 */
export default function Distributor() {
    return (
        <div className="w-20 bg-blue-500 aspect-square">
        </div>
    )
}